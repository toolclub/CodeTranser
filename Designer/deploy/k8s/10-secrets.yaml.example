# 复制为 10-secrets.yaml,填实际值,然后 kubectl apply。绝不 commit 这份。
# 生产建议用 SealedSecret / ExternalSecret / Vault 替代。
apiVersion: v1
kind: Secret
metadata:
  name: cascade-secrets
  namespace: cascade
type: Opaque
stringData:
  LLM_API_KEY: "sk-replace-me"
  MYSQL_ROOT_PASSWORD: "change-me-strong-root"
  MYSQL_PASSWORD: "change-me-strong-app"
